Вариант 2. Двухпроходный алгоритм с использованием блоков

Hot to run:
prepare & compile source code:
    1) Maven Lifcycle clean;
    2) Maven LifeCycle package;
    3)  rm -rf tmp/ && rm -rf < C dir> && $HADOOP_HOME/bin/hadoop jar target/mm.jar mm  <A dir> <B Dir>  <C dir>


